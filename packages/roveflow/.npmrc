# Auth comes from the NPM_TOKEN env var — no `npm login` needed.
# Create an Automation token at npmjs.com → Access Tokens, then:
#   export NPM_TOKEN=npm_xxxxxxxx && make publish-npm
//registry.npmjs.org/:_authToken=${NPM_TOKEN}
