name: roveflow
version: 0.1.1
description: AI-first E2E test orchestration framework for Flutter. CLI installer + framework core.
repository: https://github.com/roveflow/roveflow
homepage: https://roveflow.dev
environment:
  sdk: ^3.3.0

executables:
  roveflow: roveflow
  rf: roveflow

dependencies:
  args: ^2.5.0
  path: ^1.9.0

dev_dependencies:
  test: ^1.25.0
  lints: ^4.0.0
